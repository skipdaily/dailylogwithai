# Allow the build to continue even with peer dependency warnings
legacy-peer-deps=true

# Disable fund messages during installation
fund=false

# Disable audit during installation to speed up the process
audit=false
